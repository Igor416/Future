export default function Groups({label}) {
  return <div id="general_info" style={{backgroundColor: 'var(--mango)', height: '50vh'}} className="d-flex justify-content-center align-items-center">
    <span className="h2">{label}</span>
  </div>
}